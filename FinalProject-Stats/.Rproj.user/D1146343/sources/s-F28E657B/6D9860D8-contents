prop.table(table(video_df$category))

video_categories %>% 
  mutate(likes_prop = likes_sum /(sum(likes_sum)))

cat_df <- cat_prop %>% 
  as.data.frame()%>%
  rename(category = Var1,
         prop = Freq)



